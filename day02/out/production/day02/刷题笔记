题号 572
思路
判断一个树t是否是另一个树s的子树
取巧：先用同样的方法遍历完这两棵树，并将它们的节点值串成两个字符串（tStr,sStr），判断tStr是否是sStr的子串
递归：
一般树的问题都会使用递归，递归的回归条件是两个节点的值不等或者其中一个节点为空另一个节点不空时
返回false，如果两个节点同时为空则返回true
如果s树的节点值不等于t树的节点值，应该让s的左子树和右子树继续与t比较，只要左子树和右子树中有一个包含t即可返回true
**************************************************************************************************
题号 3
思路
返回一个字符串中无重复子串的长度，见代码，就是按照自己平常思考的思路写出来即可，关键在于利用
一个哈希表，并且处理这种abba字符串

更暴力的思路
只要出现重复的字符就将第二个字符之前的字符全部移除，需要利用一个set集合，不过不断的移除和添加，会增加时间
